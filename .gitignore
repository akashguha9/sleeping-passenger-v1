backup/
runtime/
audit_bundle/
audit_bundle.zip
__pycache__/
*.pyc
moltbook/open_positions.json

# logs/ contains runtime-generated artifacts only: pytest cache/tmp
# (per pytest.ini), chronology SQLite DB, and snapshot jsonl streams.
# Nothing in logs/ is source-controlled.
logs/
